{
  "signal": "SIGHUP",
  "env": {
    "NODE_ENV": "development"
  },
  "watch": ["favicon.ico", "src/"],
  "ext": "ts, js, txt, json, ico",
  "delay": 0,
  "exec": "tsc && node ./dist/index.js || exit 1",
  "_C": "--inspet a node-hoz?"
}
